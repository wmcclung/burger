<h1>Burgeh Burgeh</h1>

<h2>Burgers ready to be eaten</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>So Good...</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a delicious Burgeh</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Name of Burgeh</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="devoured"Have you already eaten this?></label><br>
    <input type="radio" name="devoured" value="1" checked> Yes i ate this already<br>
    <input type="radio" name="devoured" value="0"> No im hungry
  </div>

  <button type="submit">Add Burgeh</button>
</form>
